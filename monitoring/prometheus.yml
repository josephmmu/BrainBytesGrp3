global:
  scrape_interval: 5s  # Scrape every 5 seconds

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['backend:9080']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
